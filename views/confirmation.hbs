<div class="container thank-you">
  <div class="row">
    <div class="col">
    </div>
    <div class="col">
			<div class="card mt-5 thankyou" style="width: 30rem;">
			  <div class="card-body thankyou ">
					<div type="hidden" name="{{booking._id}}" value="{{booking._id}}"></div>
			    <h5 class="confirmation card-title mb-3">Appointment confirmed</h5>
			    <h6 class="confirmation card-subtitle mb-3">You're booked in!</h6>
			    <p class="card-text mb-3">See you on Sunday at {{booking.time}} for a {{booking.service}}</p>
					<div class="back-button m-5">
						<a class="btn btn-outline-secondary" href="/">Back</a>
					</div>
				</div>
			  </div>
    </div>
    <div class="col">
    </div>
  </div>
